# Traefik static configuration
entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https

  websecure:
    address: ":443"

api:
  dashboard: true

providers:
  docker: {}
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true

tls:
  options:
    default:
      minVersion: "VersionTLS12"
